apiVersion: v1
kind: Service
metadata:
  name: postgres-auth-service-deployment
  namespace: toggle-master
  labels:
    app: postgres-auth-service-deployment
spec:
  type: ClusterIP
  selector:
    app: postgres-auth-service-deployment
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
